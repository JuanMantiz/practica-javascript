//Diferencia entre un metodo y una funcion

const num1 = 20;
const num2 = "20";

console.log(parseInt(num2));
console.log(num1.toString());

// Un método es una función que pertenece a un objeto, mientras que una función es un bloque de código que se puede llamar desde cualquier parte del programa.
